[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

  [build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # ده الأمر اللي هيخلي النتفلاي يتجاهل الـ lockfile ويشتغل غصب عنه
  NPM_FLAGS = "--no-frozen-lockfile"